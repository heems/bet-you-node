extends layout

block content
  .splash
    h2 Submit A Response
    .error
      //-p #{message}
      - if (typeof(message) !== 'undefined'){
        each err in message
          p= err
      - }
    form(name="response", method="post", action=path)
      br
      p Link To Youtube Video
      input.form-control(type="text", placeholder="https://www.youtube.com/watch?v=...", name="url") 
      br
      br
      button.btn(type="submit") submit
  
